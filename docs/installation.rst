.. highlight:: shell

============
Installation
============

At the command line::

    $ easy_install zero-netcat

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv zero-netcat
    $ pip install zero-netcat
